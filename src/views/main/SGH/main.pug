//- Brand-specific markup for SGH.
//-
//- The module's shared skeleton lives in src/views/main/main.pug. Add here only
//- what differs for this brand; the file is rendered without pug locals, so no
//- `bannerName` / `crossoriginAttr` are available.
//-
//- SGH needs no brand-specific markup: the table is identical across brands and
//- only the design tokens (src/scss/variants/SGH/_variables.scss) and the
//- content (src/json/variants/SGH/json.json) change.
